# 90° RF Pulse #
    # → 1 spin
    # → 1 spin + ΔB0 offset
    # → 2 spins 
    # → 2 spins + ΔB0 offset 

# 180° RF Pulse #
    # → 1 spin
    # → 1 spin + ΔB0 offset
    # → 2 spins 
    # → 2 spins + ΔB0 offset 